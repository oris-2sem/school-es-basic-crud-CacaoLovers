spring:
  datasource:
    password: marsel55
    username: postgres
    url: jdbc:postgresql://localhost:5432/javalab-2023
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 2
      schema: oris_hw
      auto-commit: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


